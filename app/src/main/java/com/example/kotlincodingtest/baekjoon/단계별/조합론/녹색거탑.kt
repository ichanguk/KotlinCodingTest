package com.example.kotlincodingtest.baekjoon.단계별.조합론

import java.io.BufferedReader

fun main() = with(BufferedReader(System.`in`.bufferedReader())) {
    println( Math.pow(2.0, readLine().toDouble()).toInt())
}